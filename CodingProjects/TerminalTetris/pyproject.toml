[project]
name = "terminaltetris"
version = "0.1.0"
description = "A Tetris game based in the terminal. Run with py main.py in the folder that it's installed in."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "windows-curses>=2.4.2",
]
