This folder is meant to showcase the game "Tetris", playable in the terminal. To run it, open the file in a terminal and run py main.py to play.

To open terminal: win key, type "cmd"

Navigate to the folder using: cd (folder-path)/TerminalTetris

Run using: py main.py